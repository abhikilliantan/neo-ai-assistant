[project]
name = "neo-shared"
version = "0.1.0"
description = "Neo AI Assistant — shared Python contracts (transport, DTOs)."
requires-python = ">=3.12,<3.13"
readme = "README.md"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/neo_shared"]
