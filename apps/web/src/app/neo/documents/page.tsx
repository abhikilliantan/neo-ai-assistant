import { DocumentsView } from "@/features/redesign/documents/DocumentsView";

export const metadata = { title: "Documents — NEO" };

export default function DocumentsPage() {
  return <DocumentsView />;
}
